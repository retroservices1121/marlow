# Postgres connection string.
#
# Unset locally  -> the app falls back to PGlite (in-process Postgres, no setup).
# Set            -> node-postgres connects to the real database.
#
# On Railway, adding the Postgres plugin injects DATABASE_URL automatically;
# you do not need to set it by hand there.
DATABASE_URL=
