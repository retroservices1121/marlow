{
  "//": "Harness build. Inherits the app's type rules so the checks cannot pass under looser settings than the code they verify; only the module format differs, because the harnesses run under plain Node.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "module": "commonjs",
    "moduleResolution": "node",
    "noEmit": false,
    "incremental": false,
    "plugins": []
  },
  "include": [
    "scripts/**/*.ts",
    "scripts/**/*.tsx",
    "lib/**/*.ts",
    "components/**/*.tsx",
    "app/**/*.ts",
    "app/**/*.tsx"
  ],
  "exclude": ["node_modules", ".next", ".verify-out"]
}
